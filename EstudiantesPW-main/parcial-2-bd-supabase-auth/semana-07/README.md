# Semana 7 (5-9 oct) — Programación Web

**Tipo:** Individual

**Tema oficial:** U2·AE2 Modelo-Vista-Controlador (MVC), servidor de aplicaciones, métodos GET/POST.

**Objetivo:** entender el patrón MVC y aplicarlo en un ejercicio aislado antes de extender el backend real.

**Prerrequisitos:** semana 6.

**Actividad:** refactorizar el mini-servidor Express de la semana 6 separando rutas/controladores/modelo (carpetas `routes/`, `controllers/`, `models/`), agregando 1 endpoint GET y 1 POST nuevos.

**Entregable:** mini-servidor reestructurado en MVC + diagrama de las 3 capas.

**Archivos base:** `base/mini-servidor-express/` (carpetas `routes/`, `controllers/`, `models/` con andamiaje vacío).

---
La rúbrica de esta práctica la tiene tu docente por separado.
