# Semana 5 (21-25 sep) — Programación Web

**Tipo:** Integradora · **CORTE 1**

**Tema oficial:** U2·AE1 cierre + arranque de JavaScript en Node.

**Objetivo:** entregar formalmente la landing page (HTML+CSS, ya construida en semana 4) como Corte 1, y dar el primer paso fuera del navegador: ejercicios de Node.js con las funciones de JS que más se usan en el resto del curso.

**Prerrequisitos:** semana 4 (landing page + validación con regex).

**Actividad:**
1. **Corte 1 (checkpoint, sin trabajo técnico nuevo):** confirma que tu landing page de semana 4 está completa, navegable y coherente con el documento de arquitectura de Distribuidos y la estrategia de pruebas de Pruebas de Software, entregados la misma semana.
2. **Arranque de Node.js:** resuelve los 3 scripts de práctica que cubren las funciones de JS más usadas de aquí en adelante — arreglos (`map`/`filter`/`reduce`/`find`/`forEach`), objetos y JSON (`Object.keys`/`values`/`entries`, `JSON.stringify`/`parse`), y strings/fechas (`trim`/`split`/`includes`/`replace`, `Date`).

**Entregable (CORTE 1, compartido con las otras 2 materias):** landing page completa (ya construida) + los 3 scripts de Node corriendo sin errores.

**Archivos base:** `base/ejercicios-node/01-arreglos.js`, `02-objetos-json.js`, `03-strings-fechas.js` (andamiaje con `TODO`, complétalos tú y verifícalos corriendo `node <archivo>.js`).

---
La rúbrica de esta práctica la tiene tu docente por separado.
