# Semana 4 (14-18 sep) — Programación Web

**Tipo:** Integradora

**Tema oficial:** U2·AE1 HTML + CSS, primeras páginas estáticas.

**Objetivo:** aplicar CSS (incluyendo diseño responsivo básico) al HTML de la semana 3 para tener la landing page completa, y dar el primer paso a JavaScript validando los campos del formulario de registro con expresiones regulares.

**Prerrequisitos:** semana 3.

**Actividad:** estilizar con CSS (o un framework ligero) las páginas maquetadas; validar responsividad (móvil/escritorio). Además, en el formulario de registro (`registro.html`), agrega validación en JavaScript con expresiones regulares para 3 tipos de campo: texto (nombre), numérico (boleta) y fecha (DD/MM/AAAA).

**Entregable:** landing page completa (HTML+CSS) navegable localmente + capturas en móvil y escritorio + formulario de registro con validación JS funcionando (mensajes de error por campo).

**Archivos base:** `base/index.html`, `base/detalle.html`, `base/registro.html` (tu HTML de la semana 3, ya con los campos y `<span>` de error listos) + `base/styles.css` (vacío, complétalo tú) + `base/validaciones.js` (andamiaje con los 3 patrones de regex como `TODO`, complétalos tú).

---
La rúbrica de esta práctica la tiene tu docente por separado.
